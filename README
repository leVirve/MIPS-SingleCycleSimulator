This is a simulator of a CPU running. 

The input data should be "iimage.bin" & "dimage".

There are "error_dump.rpt" & "snapshot.rpt" of the retprt data.


This programe is able to read instructures of MIPS form.

And do the work they asks for on the virtual memory and registers.

Then send out a report to show the status it runs, and another report to show all errors if any.


--project done by archi37 group.
